{
  "dependencies": {
    "gh-pages": "^3.2.3"
  },

  "homepage": "https://RvN-R.github.io/zener-app",

  "scripts": {
    "deploy": "gh-pages -d build",
    "predeploy": "npm run build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}
